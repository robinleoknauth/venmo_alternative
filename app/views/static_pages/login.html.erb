Here we set a cookie. 
We can cheat and set the cookie to whatever the params[:login_id] gets
So /login?id=1 would log you in as user_id 1:::::

?id=<%= params[:id] %>

<% session.delete(:id) if params[:id].nil? %>
<% session[:id] = params[:id] %>

Session: <%= session[:id] %>